#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>

int _putchar (char c);
int _printf(const char *format, ...);
int print_str(char *str1);
int abs(int x);
int _pow(int a, int b);
int print_int(int n);

#endif

#ifndef STDLIB_H
#define STDLIB_H

#include <stdlib.h>

#endif
